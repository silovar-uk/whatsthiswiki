{
  "name": "whatsthiswiki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply whatsthiswiki --local",
    "db:migrate:remote": "wrangler d1 migrations apply whatsthiswiki --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
